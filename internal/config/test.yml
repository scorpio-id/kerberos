# server configuration
server:
  host: "http://127.0.0.1"
  port: 8083

oauth:
  enabled: True
  trusted_issuers:
    - "http://localhost:8082/jwks"

realm:
  name: "SCORPIO.IO"
  password_rotation: "1h"