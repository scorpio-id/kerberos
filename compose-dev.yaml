services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: docker/dev-environments-go:stable-1
    init: true
    ports:
     - 8083:8083
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

